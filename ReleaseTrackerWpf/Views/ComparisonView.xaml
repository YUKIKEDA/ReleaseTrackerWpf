<UserControl
    x:Class="ReleaseTrackerWpf.Views.ComparisonView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:ReleaseTrackerWpf.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="clr-namespace:ReleaseTrackerWpf.Models"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:viewmodels="clr-namespace:ReleaseTrackerWpf.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewmodels:ComparisonViewModel}"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Snapshot Add Button (Above Grid)  -->
        <ui:Button
            Grid.Row="0"
            Margin="0,10,0,10"
            HorizontalAlignment="Right"
            Command="{Binding AddSnapshotCommand}"
            Content="スナップショット追加"
            Visibility="{Binding HasSnapshots, Converter={StaticResource BooleanToVisibilityConverter}}" />

        <!--  Control Panel  -->
        <ui:Card Grid.Row="1" Margin="0,0,0,10">
            <Grid Margin="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!--  Old Header  -->
                <TextBlock
                    Grid.Row="0"
                    Grid.Column="0"
                    Margin="0,0,0,10"
                    FontSize="16"
                    FontWeight="Bold"
                    Text="比較元"
                    Visibility="{Binding HasSnapshots, Converter={StaticResource BooleanToVisibilityConverter}}" />

                <!--  Old Structure Selection  -->
                <StackPanel
                    Grid.Row="1"
                    Grid.Column="0"
                    Margin="0,0,10,0">
                    <ComboBox
                        Margin="0,0,0,10"
                        ItemsSource="{Binding AvailableSnapshots}"
                        SelectedItem="{Binding SelectedOldSnapshot}"
                        Visibility="{Binding HasSnapshots, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock FontWeight="Bold" Text="{Binding RootPath}" />
                                    <TextBlock
                                        FontSize="11"
                                        Foreground="Gray"
                                        Text="{Binding CreatedAt, StringFormat='作成日時: {0:yyyy/MM/dd HH:mm}'}" />
                                </StackPanel>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </StackPanel>

                <!--  New Header  -->
                <TextBlock
                    Grid.Row="0"
                    Grid.Column="1"
                    Margin="0,0,0,10"
                    FontSize="16"
                    FontWeight="Bold"
                    Text="比較先"
                    Visibility="{Binding HasSnapshots, Converter={StaticResource BooleanToVisibilityConverter}}" />

                <!--  New Structure Selection  -->
                <StackPanel
                    Grid.Row="1"
                    Grid.Column="1"
                    Margin="10,0,0,0">
                    <ComboBox
                        Margin="0,0,0,10"
                        ItemsSource="{Binding AvailableSnapshots}"
                        SelectedItem="{Binding SelectedNewSnapshot}"
                        Visibility="{Binding HasSnapshots, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock FontWeight="Bold" Text="{Binding RootPath}" />
                                    <TextBlock
                                        FontSize="11"
                                        Foreground="Gray"
                                        Text="{Binding CreatedAt, StringFormat='作成日時: {0:yyyy/MM/dd HH:mm}'}" />
                                </StackPanel>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </StackPanel>

                <!--  First Time Snapshot Creation Button (Centered)  -->
                <StackPanel
                    Grid.Row="0"
                    Grid.Column="0"
                    Grid.ColumnSpan="2"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Visibility="{Binding HasNoSnapshots, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <ui:Button
                        Padding="40,20"
                        HorizontalAlignment="Center"
                        Command="{Binding AddSnapshotCommand}"
                        Content="スナップショット作成"
                        FontSize="18"
                        FontWeight="Bold" />
                    <TextBlock
                        Margin="0,10,0,0"
                        HorizontalAlignment="Center"
                        FontSize="14"
                        Foreground="Gray"
                        Text="フォルダを選択してスナップショットを作成してください" />
                </StackPanel>

            </Grid>
        </ui:Card>

        <!--  Results Area  -->
        <ui:Card Grid.Row="2" VerticalAlignment="Top">
            <Grid Margin="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <StackPanel
                    Grid.Row="0"
                    Grid.ColumnSpan="2"
                    Margin="0,0,0,10"
                    Orientation="Horizontal">
                    <TextBlock
                        VerticalAlignment="Center"
                        FontSize="16"
                        FontWeight="Bold"
                        Text="比較結果" />
                    <StackPanel Margin="20,0,0,0" Orientation="Horizontal">
                        <ui:Button
                            Margin="0,0,5,0"
                            Command="{Binding CompareDirectoryCommand}"
                            Content="スキャン実行" />
                        <ui:Button
                            Margin="5,0,0,0"
                            Command="{Binding ExportComparisonExcelCommand}"
                            Content="比較結果 エクスポート"
                            IsEnabled="{Binding HasComparisonResult}" />
                    </StackPanel>
                </StackPanel>

                <StackPanel
                    Grid.Row="1"
                    Grid.Column="0"
                    Margin="0,0,0,10"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">
                    <ui:Button
                        Margin="5,0,5,0"
                        Command="{Binding ExportOldSnapshotCommand}"
                        Content="CSVエクスポート" />
                    <ui:Button
                        Margin="5,0,0,0"
                        Command="{Binding ImportOldDescriptionsCommand}"
                        Content="説明インポート" />
                </StackPanel>

                <StackPanel
                    Grid.Row="1"
                    Grid.Column="1"
                    Margin="0,0,0,10"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">
                    <ui:Button
                        Margin="5,0,5,0"
                        Command="{Binding ExportNewSnapshotCommand}"
                        Content="CSVエクスポート" />
                    <ui:Button
                        Margin="5,0,0,0"
                        Command="{Binding ImportNewDescriptionsCommand}"
                        Content="説明インポート" />
                </StackPanel>

                <!--  Comparison Results ListView (Old)  -->
                <ui:ListView
                    x:Name="LeftListView"
                    Grid.Row="2"
                    Grid.Column="0"
                    Margin="0,0,5,0"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    ItemsSource="{Binding OldFlattenedDirectoryStructure}"
                    PreviewMouseWheel="ScrollViewer_PreviewMouseWheel"
                    ScrollViewer.HorizontalScrollBarVisibility="Auto"
                    ScrollViewer.ScrollChanged="LeftScrollViewer_ScrollChanged"
                    ScrollViewer.VerticalScrollBarVisibility="Auto"
                    Visibility="{Binding BothSnapshotsSelected, Converter={StaticResource BooleanToVisibilityConverter}}"
                    VirtualizingPanel.IsVirtualizing="True"
                    VirtualizingPanel.VirtualizationMode="Recycling">
                        <ui:ListView.ItemTemplate>
                            <DataTemplate DataType="{x:Type viewmodels:FileItemViewModel}">
                                <StackPanel Orientation="Horizontal">
                                    <!--  ツリー構造の罫線  -->
                                    <TextBlock
                                        VerticalAlignment="Center"
                                        FontFamily="Consolas"
                                        Foreground="{Binding DifferenceType, Converter={StaticResource DifferenceTypeToForegroundConverter}}"
                                        Text="{Binding TreePrefix}" />
                                    <!--  フォルダ/ファイルアイコン  -->
                                    <ui:SymbolIcon
                                        Margin="0,0,5,0"
                                        VerticalAlignment="Center"
                                        FontSize="14"
                                        Foreground="{Binding DifferenceType, Converter={StaticResource DifferenceTypeToForegroundConverter}}"
                                        Symbol="{Binding IsDirectory, Converter={StaticResource FolderFileIconConverter}}" />
                                    <TextBlock
                                        VerticalAlignment="Center"
                                        Foreground="{Binding DifferenceType, Converter={StaticResource DifferenceTypeToForegroundConverter}}"
                                        Text="{Binding Name}" />
                                    <TextBlock
                                        Margin="10,0,0,0"
                                        VerticalAlignment="Center"
                                        FontSize="11"
                                        Foreground="{Binding DifferenceType, Converter={StaticResource DifferenceTypeToForegroundConverter}}"
                                        Text="{Binding Size, StringFormat='({0:N0} bytes)'}"
                                        Visibility="{Binding IsDirectory, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Invert}" />
                                    <!--  説明文の表示  -->
                                    <TextBlock
                                        Margin="5,0,0,0"
                                        VerticalAlignment="Center"
                                        FontSize="11"
                                        Foreground="{Binding DifferenceType, Converter={StaticResource DifferenceTypeToForegroundConverter}}"
                                        Text="{Binding DisplayDescription}"
                                        Visibility="{Binding DisplayDescription, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                </StackPanel>
                            </DataTemplate>
                        </ui:ListView.ItemTemplate>
                        <ui:ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="Padding" Value="4,2" />
                                <Setter Property="Margin" Value="0,1" />
                                <Setter Property="Background" Value="{Binding DifferenceType, Converter={StaticResource DifferenceTypeToColorConverter}, ConverterParameter=left}" />
                                <Setter Property="Foreground" Value="{StaticResource TreeViewItemForeground}" />
                                <Setter Property="BorderThickness" Value="0" />
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                <Style.Triggers>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}" Value="True" />
                                            <Condition Binding="{Binding IsDirectory}" Value="True" />
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="Background" Value="{Binding DifferenceType, Converter={StaticResource DifferenceTypeToColorConverter}, ConverterParameter=left hover}" />
                                        <Setter Property="Foreground" Value="{StaticResource TreeViewItemForeground}" />
                                    </MultiDataTrigger>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}" Value="True" />
                                            <Condition Binding="{Binding IsDirectory}" Value="False" />
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="Background" Value="{Binding DifferenceType, Converter={StaticResource DifferenceTypeToColorConverter}, ConverterParameter=left hover}" />
                                        <Setter Property="Foreground" Value="{StaticResource TreeViewItemForeground}" />
                                    </MultiDataTrigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="{Binding DifferenceType, Converter={StaticResource DifferenceTypeToColorConverter}, ConverterParameter=left}" />
                                        <Setter Property="Foreground" Value="{StaticResource TreeViewItemForeground}" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ui:ListView.ItemContainerStyle>
                </ui:ListView>

                <!--  Comparison Results ListView (New)  -->
                <ui:ListView
                    x:Name="RightListView"
                    Grid.Row="2"
                    Grid.Column="1"
                    Margin="5,0,0,0"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    ItemsSource="{Binding NewFlattenedDirectoryStructure}"
                    PreviewMouseWheel="ScrollViewer_PreviewMouseWheel"
                    ScrollViewer.HorizontalScrollBarVisibility="Auto"
                    ScrollViewer.ScrollChanged="RightScrollViewer_ScrollChanged"
                    ScrollViewer.VerticalScrollBarVisibility="Auto"
                    Visibility="{Binding BothSnapshotsSelected, Converter={StaticResource BooleanToVisibilityConverter}}"
                    VirtualizingPanel.IsVirtualizing="True"
                    VirtualizingPanel.VirtualizationMode="Recycling">
                        <ui:ListView.ItemTemplate>
                            <DataTemplate DataType="{x:Type viewmodels:FileItemViewModel}">
                                <StackPanel Orientation="Horizontal">
                                    <!--  ツリー構造の罫線  -->
                                    <TextBlock
                                        VerticalAlignment="Center"
                                        FontFamily="Consolas"
                                        Foreground="{Binding DifferenceType, Converter={StaticResource DifferenceTypeToForegroundConverter}}"
                                        Text="{Binding TreePrefix}" />
                                    <!--  フォルダ/ファイルアイコン  -->
                                    <ui:SymbolIcon
                                        Margin="0,0,5,0"
                                        VerticalAlignment="Center"
                                        FontSize="14"
                                        Foreground="{Binding DifferenceType, Converter={StaticResource DifferenceTypeToForegroundConverter}}"
                                        Symbol="{Binding IsDirectory, Converter={StaticResource FolderFileIconConverter}}" />
                                    <TextBlock
                                        VerticalAlignment="Center"
                                        Foreground="{Binding DifferenceType, Converter={StaticResource DifferenceTypeToForegroundConverter}}"
                                        Text="{Binding Name}" />
                                    <TextBlock
                                        Margin="10,0,0,0"
                                        VerticalAlignment="Center"
                                        FontSize="11"
                                        Foreground="{Binding DifferenceType, Converter={StaticResource DifferenceTypeToForegroundConverter}}"
                                        Text="{Binding Size, StringFormat='({0:N0} bytes)'}"
                                        Visibility="{Binding IsDirectory, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Invert}" />
                                    <!--  説明文の表示  -->
                                    <TextBlock
                                        Margin="5,0,0,0"
                                        VerticalAlignment="Center"
                                        FontSize="11"
                                        Foreground="{Binding DifferenceType, Converter={StaticResource DifferenceTypeToForegroundConverter}}"
                                        Text="{Binding DisplayDescription}"
                                        Visibility="{Binding DisplayDescription, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                </StackPanel>
                            </DataTemplate>
                        </ui:ListView.ItemTemplate>
                        <ui:ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="Padding" Value="4,2" />
                                <Setter Property="Margin" Value="0,1" />
                                <Setter Property="Background" Value="{Binding DifferenceType, Converter={StaticResource DifferenceTypeToColorConverter}, ConverterParameter=right}" />
                                <Setter Property="Foreground" Value="{StaticResource TreeViewItemForeground}" />
                                <Setter Property="BorderThickness" Value="0" />
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                <Style.Triggers>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}" Value="True" />
                                            <Condition Binding="{Binding IsDirectory}" Value="True" />
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="Background" Value="{Binding DifferenceType, Converter={StaticResource DifferenceTypeToColorConverter}, ConverterParameter=right hover}" />
                                        <Setter Property="Foreground" Value="{StaticResource TreeViewItemForeground}" />
                                    </MultiDataTrigger>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsMouseOver}" Value="True" />
                                            <Condition Binding="{Binding IsDirectory}" Value="False" />
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="Background" Value="{Binding DifferenceType, Converter={StaticResource DifferenceTypeToColorConverter}, ConverterParameter=right hover}" />
                                        <Setter Property="Foreground" Value="{StaticResource TreeViewItemForeground}" />
                                    </MultiDataTrigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="{Binding DifferenceType, Converter={StaticResource DifferenceTypeToColorConverter}, ConverterParameter=right}" />
                                        <Setter Property="Foreground" Value="{StaticResource TreeViewItemForeground}" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ui:ListView.ItemContainerStyle>
                </ui:ListView>
            </Grid>
        </ui:Card>

        <!--  比較進捗ダイアログオーバーレイ  -->
        <Grid
            Grid.RowSpan="3"
            Background="#80000000"
            Visibility="{Binding IsComparison, Converter={StaticResource BooleanToVisibilityConverter}}">
            <ui:Card
                Width="300"
                Height="150"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ui:ProgressRing
                        Width="48"
                        Height="48"
                        Margin="0,0,0,20"
                        IsIndeterminate="True" />
                    <TextBlock
                        HorizontalAlignment="Center"
                        FontSize="16"
                        FontWeight="SemiBold"
                        Text="{Binding ProgressMessage}" />
                    <TextBlock
                        Margin="0,5,0,0"
                        HorizontalAlignment="Center"
                        FontSize="12"
                        Foreground="Gray"
                        Text="しばらくお待ちください" />
                </StackPanel>
            </ui:Card>
        </Grid>
    </Grid>
</UserControl>
