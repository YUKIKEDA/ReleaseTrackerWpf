<ui:FluentWindow x:Class="ReleaseTrackerWpf.Views.ComparisonWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:converters="clr-namespace:ReleaseTrackerWpf.Converters"
                 mc:Ignorable="d"
                 Title="ディレクトリ構造比較"
                 Height="800" Width="1400"
                 MinHeight="600" MinWidth="1000"
                 WindowStartupLocation="CenterScreen">

    <ui:FluentWindow.Resources>
        <converters:DifferenceTypeToColorConverter x:Key="DifferenceTypeToColorConverter"/>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <!-- 削除されたアイテムのスタイル -->
        <Style x:Key="DeletedItemStyle" TargetType="TreeViewItem">
            <Setter Property="Background" Value="#FFE6E6"/>
            <Setter Property="BorderBrush" Value="#FFB3B3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,1"/>
        </Style>

        <!-- 追加されたアイテムのスタイル -->
        <Style x:Key="AddedItemStyle" TargetType="TreeViewItem">
            <Setter Property="Background" Value="#E6FFE6"/>
            <Setter Property="BorderBrush" Value="#B3FFB3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,1"/>
        </Style>

        <!-- 変更されたアイテムのスタイル -->
        <Style x:Key="ModifiedItemStyle" TargetType="TreeViewItem">
            <Setter Property="Background" Value="#FFF2E6"/>
            <Setter Property="BorderBrush" Value="#FFE0B3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,1"/>
        </Style>
    </ui:FluentWindow.Resources>

    <Grid Background="{StaticResource ApplicationBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <ui:TitleBar Grid.Row="0" Title="ディレクトリ構造比較" Height="28" Background="{StaticResource TitleBarBackgroundBrush}"
                     FontWeight="Bold" FontSize="14">
            <ui:TitleBar.Icon>
                <ui:SymbolIcon Symbol="DocumentText16" FontSize="18"/>
            </ui:TitleBar.Icon>
        </ui:TitleBar>

        <!-- ヘッダー情報 -->
        <ui:Card Grid.Row="1" Margin="10,10,10,0">
            <Grid Margin="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- 旧構造情報 -->
                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock Text="旧構造 (比較元)" FontWeight="Bold" FontSize="16" Foreground="#B40000"/>
                    <TextBlock Text="{Binding OldSnapshot.RootPath}" FontSize="12" Margin="0,5,0,0"/>
                    <TextBlock Text="{Binding OldSnapshot.CreatedAt, StringFormat='作成日時: {0:yyyy/MM/dd HH:mm}'}"
                               FontSize="10" Foreground="Gray" Margin="0,2,0,0"/>
                </StackPanel>

                <!-- 新構造情報 -->
                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                    <TextBlock Text="新構造 (比較先)" FontWeight="Bold" FontSize="16" Foreground="#007800"/>
                    <TextBlock Text="{Binding NewSnapshot.RootPath}" FontSize="12" Margin="0,5,0,0"/>
                    <TextBlock Text="{Binding NewSnapshot.CreatedAt, StringFormat='作成日時: {0:yyyy/MM/dd HH:mm}'}"
                               FontSize="10" Foreground="Gray" Margin="0,2,0,0"/>
                </StackPanel>
            </Grid>
        </ui:Card>

        <!-- メイン比較エリア -->
        <Grid Grid.Row="2" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 左側：旧構造 -->
            <ui:Card Grid.Column="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="#FFE6E6" Padding="10">
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="Delete16" Foreground="#B40000" Margin="0,0,8,0"/>
                            <TextBlock Text="旧構造 (削除されたファイル)" FontWeight="Bold" Foreground="#B40000"/>
                        </StackPanel>
                    </Border>

                    <TreeView Grid.Row="1"
                              ItemsSource="{Binding OldStructureTree}"
                              Margin="10"
                              ScrollViewer.HorizontalScrollBarVisibility="Auto">
                        <TreeView.ItemTemplate>
                            <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                                <StackPanel Orientation="Horizontal">
                                    <!-- ファイル/フォルダアイコン -->
                                    <ui:SymbolIcon Symbol="Document16" FontSize="12" Margin="0,0,5,0">
                                        <ui:SymbolIcon.Style>
                                            <Style TargetType="ui:SymbolIcon">
                                                <Setter Property="Symbol" Value="Document16"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsDirectory}" Value="True">
                                                        <Setter Property="Symbol" Value="Folder16"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </ui:SymbolIcon.Style>
                                    </ui:SymbolIcon>

                                    <!-- 差分アイコン -->
                                    <ui:SymbolIcon Symbol="Delete16" FontSize="10" Margin="0,0,5,0"
                                                   Foreground="#B40000"
                                                   Visibility="{Binding IsDeleted, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                   ToolTip="削除されたファイル"/>

                                    <!-- ファイル名 -->
                                    <TextBlock Text="{Binding Name}" VerticalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsDeleted}" Value="True">
                                                        <Setter Property="Foreground" Value="#B40000"/>
                                                        <Setter Property="TextDecorations" Value="Strikethrough"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>

                                    <!-- ファイルサイズ -->
                                    <TextBlock Text="{Binding DisplaySize}"
                                               Margin="10,0,0,0"
                                               FontSize="10"
                                               Foreground="Gray"
                                               VerticalAlignment="Center"/>
                                </StackPanel>
                            </HierarchicalDataTemplate>
                        </TreeView.ItemTemplate>
                    </TreeView>
                </Grid>
            </ui:Card>

            <!-- 中央の区切り線 -->
            <GridSplitter Grid.Column="1"
                          VerticalAlignment="Stretch"
                          HorizontalAlignment="Stretch"
                          Background="Gray"
                          ResizeBehavior="PreviousAndNext"/>

            <!-- 右側：新構造 -->
            <ui:Card Grid.Column="2">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="#E6FFE6" Padding="10">
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="Add16" Foreground="#007800" Margin="0,0,8,0"/>
                            <TextBlock Text="新構造 (追加・変更されたファイル)" FontWeight="Bold" Foreground="#007800"/>
                        </StackPanel>
                    </Border>

                    <TreeView Grid.Row="1"
                              ItemsSource="{Binding NewStructureTree}"
                              Margin="10"
                              ScrollViewer.HorizontalScrollBarVisibility="Auto">
                        <TreeView.ItemTemplate>
                            <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                                <StackPanel Orientation="Horizontal">
                                    <!-- ファイル/フォルダアイコン -->
                                    <ui:SymbolIcon Symbol="Document16" FontSize="12" Margin="0,0,5,0">
                                        <ui:SymbolIcon.Style>
                                            <Style TargetType="ui:SymbolIcon">
                                                <Setter Property="Symbol" Value="Document16"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsDirectory}" Value="True">
                                                        <Setter Property="Symbol" Value="Folder16"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </ui:SymbolIcon.Style>
                                    </ui:SymbolIcon>

                                    <!-- 差分アイコン -->
                                    <ui:SymbolIcon FontSize="10" Margin="0,0,5,0">
                                        <ui:SymbolIcon.Style>
                                            <Style TargetType="ui:SymbolIcon">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsAdded}" Value="True">
                                                        <Setter Property="Symbol" Value="Add16"/>
                                                        <Setter Property="Foreground" Value="#007800"/>
                                                        <Setter Property="ToolTip" Value="追加されたファイル"/>
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding IsModified}" Value="True">
                                                        <Setter Property="Symbol" Value="Edit16"/>
                                                        <Setter Property="Foreground" Value="#C86400"/>
                                                        <Setter Property="ToolTip" Value="変更されたファイル"/>
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </Style>
                                        </ui:SymbolIcon.Style>
                                    </ui:SymbolIcon>

                                    <!-- ファイル名 -->
                                    <TextBlock Text="{Binding Name}" VerticalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsAdded}" Value="True">
                                                        <Setter Property="Foreground" Value="#007800"/>
                                                        <Setter Property="FontWeight" Value="Bold"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding IsModified}" Value="True">
                                                        <Setter Property="Foreground" Value="#C86400"/>
                                                        <Setter Property="FontWeight" Value="Bold"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>

                                    <!-- ファイルサイズ -->
                                    <TextBlock Text="{Binding DisplaySize}"
                                               Margin="10,0,0,0"
                                               FontSize="10"
                                               Foreground="Gray"
                                               VerticalAlignment="Center"/>
                                </StackPanel>
                            </HierarchicalDataTemplate>
                        </TreeView.ItemTemplate>
                    </TreeView>
                </Grid>
            </ui:Card>
        </Grid>

        <!-- ステータスバー -->
        <Border Grid.Row="3"
                Background="{StaticResource StatusBarBackgroundBrush}"
                Margin="10,0,10,10"
                CornerRadius="4">
            <Grid Margin="15,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="統計: " FontWeight="Bold"/>
                    <TextBlock Text="{Binding AddedCount}" Foreground="#007800" Margin="5,0"/>
                    <TextBlock Text="個追加" Foreground="#007800"/>
                    <TextBlock Text="," Margin="3,0"/>
                    <TextBlock Text="{Binding DeletedCount}" Foreground="#B40000" Margin="5,0"/>
                    <TextBlock Text="個削除" Foreground="#B40000"/>
                    <TextBlock Text="," Margin="3,0"/>
                    <TextBlock Text="{Binding ModifiedCount}" Foreground="#C86400" Margin="5,0"/>
                    <TextBlock Text="個変更" Foreground="#C86400"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <ui:Button Content="エクスポート" Command="{Binding ExportCommand}" Margin="0,0,10,0"/>
                    <ui:Button Content="閉じる" Click="CloseButton_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</ui:FluentWindow>